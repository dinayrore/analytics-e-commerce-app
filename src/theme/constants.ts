export enum FontFamily {
  body = "VCGaramondCondensed-Regular",
  heading = "VCGaramondCondensed-Bold",
  subheading = "VCGaramondCondensed-SemiBold",
}

export enum FontWeight {
  medium = "500",
  bold = "700",
}

export enum FontSize {
  bodyText = 16,
  heading = 24,
  subheading = 20,
  caption = 12,
}

export enum LineHeight {
  bodyText = 24,
  heading = 32,
  subheading = 64,
  caption = 14,
}

export enum TextDecorationLine {
  underline = "underline",
  none = "none",
}

export enum Colors {
  emerald = "#0e5135",
  cream = "#F9F4CE",
  sage = "#9FC184",
  skyBlue = "#97C8D9",
  turquoise = "#316570",
  errorRed = "#AC2B33",
  black = "#000000",
  midGray = "#757575",
  midMediumGray = "#B0B0B0",
  lightGray = "#EBEBEB",
  neutralGray = "#FBFBFB",
  white = "#FFFFFF",
}
