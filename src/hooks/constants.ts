/* istanbul ignore file */
export enum HookErrorMessage {
  auth = "useAuthentication hook can only be used within an AuthenticationProvider",
  products = "useProducts hook can only be used within a ProductsProvider",
  shoppingCart = "useShoppingCart hook can only be used within a ShoppingCartProvider",
}
